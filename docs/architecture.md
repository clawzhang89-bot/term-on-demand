# 架构总图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        你                                            │
│                         │                                            │
│            ┌────────────┴────────────┐                              │
│            │                         │                              │
│      Mini 键盘 (6-8键)          语音 (中/英模式键)                   │
│      Ctrl/C/V/Tab/Esc/翻页      描述需求、路径、指令                  │
│            │                         │                              │
│            └────────────┬────────────┘                              │
│                         │                                            │
│                         ▼                                            │
│                 Bluetooth                                            │
│                         │                                            │
├─────────────────────────┼─────────────────────────────────────────────┤
│                         ▼                                            │
│                 Beam Pro / 手机                                      │
│                         │                                            │
│              ┌──────────┴──────────┐                                 │
│              │                     │                                  │
│         Termius (SSH)        Chrome (浏览器)                          │
│              │                     │                                  │
│              │                     │                                  │
├──────────────┼─────────────────────┼──────────────────────────────────┤
│              │                     │                                  │
│              ▼                     ▼                                  │
│      ┌──────────────┐     ┌──────────────┐                           │
│      │  云端 Ubuntu  │     │  Nginx       │                           │
│      │  (dev host)   │     │  (静态文件)   │                           │
│      │              │     │              │                           │
│      │  terminal    │◄───►│  /var/www/   │                           │
│      │  output      │     │  *.html      │ ◄── AI 现场写在这里       │
│      │              │     │              │                           │
│      └──────┬───────┘     └──────────────┘                           │
│             │                                                         │
│             ▼                                                         │
│      ┌──────────────┐                                                 │
│      │  AI Agent    │                                                 │
│      │  Claude Code │                                                 │
│      │  Codex CLI   │                                                 │
│      │  Cloud Code  │                                                 │
│      │              │                                                 │
│      │  监听需求 → 写脚本 → 或 生成HTML                               │
│      └──────────────┘                                                 │
└─────────────────────────────────────────────────────────────────────┘

渲染层（你眼前的画面）：
┌─────────────────────────────────────────────┐
│  XREAL One Pro 眼镜                          │
│                                             │
│  默认：全屏终端界面（黑底绿字/白字）           │
│  按需：全屏浏览器（打开的 HTML 页面）          │
│                                             │
│  3DoF 悬停：画面固定在空间中的位置             │
│  转头不走，像一台虚拟显示器                   │
└─────────────────────────────────────────────┘

数据流向：

终端操作时：  你 → 迷你键 → Beam Pro → SSH → Ubuntu → 终端输出 →
              眼镜显示

可视化需求时： 你 → 语音 → Beam Pro → SSH → Ubuntu → AI agent 写 HTML
              → Nginx server → 浏览器 → 眼镜显示

AI agent 代操：你 → 语音 → OpenClaw → SSH → Ubuntu → 执行命令 →
              结果返回 → 眼镜/语音回传
```
